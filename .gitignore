# Local credentials and configuration
.tgcloud/
.env
.env.*
!.env.example
!.env.gpu.example
!.env.eval.example
config.local.json
handlers/_configure.js

# Private stories, exports, backups and diagnostic output
data/
# A description and a picture are made from the text of somebody's scene, so they are story data. Both default
# here: illustrations/prompts (local/illustrate-probe.ts) and illustrations/pictures (local/image-batch.ts).
illustrations/
seeds/
stories/
exports/
backups/
logs/
*.log
*.sqlite*
*.db
*.db-*
# The portraits a reader kept lie beside the database, whatever it is called (local/store.ts `portraits`).
*.portraits/

# Dependencies and generated reports
__pycache__/
node_modules/
coverage/
measurements/
models/
*.gguf
# Vocabularies written by `npm run tokenizers` (docs/tokenizers.md)
tokenizers/
